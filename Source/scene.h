extern IMeshSceneNode *obj_node_1, /* Force field 1. */
			   *obj_node_2, /* Force field 2. */
			   *obj_node_prove, /* Q. */
			   *obj_node_ball, /* Ball. */
			   *obj_node_plat, /* Platform. */
			   *obj_node_empty; /* Empty. */


/* Draw the camera. */
void DrawCamera();
void UpdateCamera();
/* Draw a object. */
void DrawForce1();
void DrawForce2();
void DrawForceProve();
void DrawBall();
void DrawPlat();